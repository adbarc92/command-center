# Handoff — run Smoke run 2 and unblock PR #49

**Written:** 2026-08-15  ·  **Branch:** `feat/plugin-runtime` @ `0466450`  ·  **Session:** `ae18cd84-95fa-45e7-a26f-d09f64a96826`

## ⏳ Background operation in flight

**None.** All background work from this session has completed. This handoff exists because the
remaining work is **blocked on a human GUI session**, not on a machine.

Last background op: PR #49 CI. **Settled, 9/9 pass** — `fmt + clippy`, `cargo test (cockpit)`,
`cargo test (workspace)`, `svelte-check + tsc`, `vitest (cockpit/ui)`, `embargo guard`, and
`tauri build` on all three OSes. Re-verify cheaply with `gh pr checks 49`.

## Goal

Run **Smoke run 2** — the interactive dev + packaged smoke — to completion, record results in
[`spikes/SPIKE-RESULTS.md`](../../spikes/SPIKE-RESULTS.md), and mark **PR #49 ready**. The smoke is
the **sole remaining merge blocker** on #49; every automated gate now passes.

## State

- **Checklist source:** [`spikes/SPIKE-RESULTS.md`](../../spikes/SPIKE-RESULTS.md) → "REMAINING HUMAN GATE" (§148) and "Smoke run 1" (§172)
- **Status doc:** [`docs/STATUS.md`](../STATUS.md) — State summary is current as of 2026-08-15
- **Done this session:**
  - PR #60 merged → `main` @ `3cc1ae6`. CI now gates fmt, clippy, type-check, vitest, **and the
    cockpit crate's tests, which had never run in CI at all.**
  - Gate-5 teardown tests + main-thread ratchet test → `0d05f55`
  - Risk-ranked testing plan → `f4d7f38` ([`docs/testing/PLAN.md`](../testing/PLAN.md), 131 gaps)
  - `main` merged up (clean) → `458b952`; fmt violations it exposed fixed → `42e290e`
  - STATUS.md rewritten → `0466450`
  - Issue #53 closed; worktrees 5→3; branches pruned
- **In flight (uncommitted):** nothing. Working tree is clean and pushed.

## Successor autonomy

**`checkpoint-each`, operator-driven — settled with the operator this session.**

The successor **drives the checklist**: state each item, say exactly what to click and what a PASS
looks like, then **wait for the operator to report what they saw**. The successor does **not** infer
results, and does **not** mark an item PASS that the operator did not explicitly confirm. Recording
results into `SPIKE-RESULTS.md` and pushing is autonomous once a result is reported.

**A "not run" item must stay "not run".** Smoke run 1's whole lesson is that unmeasured items get
laundered into passes. Nine of eleven items have never been executed.

## Successor's next action

1. Confirm the Gate-5 baseline is still valid — it is judged against this number:
   ```powershell
   docker ps --format '{{.Names}}'          # must be EMPTY (baseline was 0 containers)
   Get-Process app,fleetd-serve -ErrorAction SilentlyContinue   # must be empty
   ```
2. Launch with the dev-seam env vars set. **`npm run desktop` does NOT set them**; without them
   `ccplugin://` and app discovery won't resolve and you get a false failure:
   ```powershell
   $repo = 'D:\MajorProjects\CURRENT\command-center'
   $env:CC_VIEW_PLUGINS_DEV = "$repo\plugins"
   $env:CC_PLUGIN_SDK       = "$repo\cockpit\plugin-sdk\index.js"
   $env:CC_APP_PLUGINS_DEV  = "$repo\cockpit\ui\src-tauri\app-plugins"
   Set-Location "$repo\cockpit\ui"; npm run desktop
   ```
   (A preflighting launcher was written to this session's scratchpad at `smoke-dev.ps1`; it is
   temp-dir scoped and may be gone. The five lines above are the whole of it that matters.)
3. Drive the checklist below **in this order**. 1.5 is deliberately held until after 1.3/1.4 so a
   repeat freeze doesn't block the cheap items the way it did in run 1.

### Checklist — Part 1 (dev)

| # | Item | PASS looks like |
|---|---|---|
| 1.1 | switcher renders | Four entries: FLEET (default) + PROJECTS + REFERENCE + AUDIENCE |
| 1.2 | fleet regression canary | FLEET ops grid unchanged — tiles, colours, rails all as before |
| 1.3 | REFERENCE view-plugin | Sandboxed iframe renders; `plugin-hello` → `ready` completes; **no network** from the plugin |
| 1.4 | command policy round-trip | A policed `launch` round-trips; the `command-ack` **rejection** path also fires |
| **1.5** | **AUDIENCE activation — PIVOTAL** | Chip walks `starting → health-probing → healthy` **with the window responsive throughout**. A frozen window means `db74a47` did not take. Images are prebuilt, so no 20-min build. |
| 1.6 | rect glue on resize | Native webview stays glued to its rect while resizing |
| 1.7 | park-on-overlay | Stage a REAL launch → webview parks off-screen, restores on close |
| 1.8 | no leak on switch | AUDIENCE→FLEET→AUDIENCE: no orphaned webview, Fleet state preserved |
| 1.9a | Gate 5 — container teardown | After **graceful** quit: `docker ps` empty, ports 8787/5173/8080 released |
| 1.9b | Gate 5 — process exit | `app` process actually exits. **Known ANOMALY — see below** |
| 1.10 | HMR under host CSP | Vite HMR still works with the new host CSP |

### Checklist — Part 2 (packaged)

`npm run bundle` → run `target/release/*app*.exe` → repeat 1.1–1.9. Confirms `ccplugin://` and the
child webview work with **no dev server**. Never executed; budget real time for it.

## Live decisions (settled)

- **#60 merges before #49** — done. #49 is rebased on it and green.
- **Results go in `spikes/SPIKE-RESULTS.md` under a new "Smoke run 2" section**, same table shape as
  run 1. That file is the canonical manual-QA record.
- **Item order is 1.1→1.4, then 1.5, then 1.6→1.10**, then Part 2.
- **#49 gets marked ready only when Part 1 and Part 2 are both complete.** A partial pass does not
  unblock it.

## ⚠️ Traps that have already cost time twice

1. **Quit gracefully — window close only. NEVER `Stop-Process`.** A force-kill skips
   `stop_all_owned` and fabricates a Gate-5 teardown failure.
2. **Do not rebuild the tauri crate while the app is running.** `tauri-build` cannot overwrite
   `target/debug/fleetd-serve.exe` while it is the running sidecar and fails `PermissionDenied`.
   Quit the app first.
3. **Check `cockpit/ui/node_modules` is populated** before trusting any JS gate. It was empty at the
   start of run 1 and both JS gates failed with `'vitest' is not recognized` — a toolchain failure
   that reads as a code failure. Verified present as of this handoff.

## ⚠️ Open questions (unresolved — do NOT settle implicitly)

1. **The 1.9b process-exit anomaly is undiagnosed and must be decided this run.** In run 1 the `app`
   process survived the window close (no window, 23 threads, still responding 15 s later) *after*
   teardown had demonstrably succeeded. It is `GAP-010` in the testing plan, scored 25/25, and has
   been carried forward three times. **Decide: `tauri dev` supervision artifact, or a real shutdown
   defect?** The new `stop_all_owned_is_idempotent` test (`0d05f55`) rules out a second teardown
   pass re-spending the 30 s `ExitRequested` budget, so that hypothesis is already eliminated.
   If it reproduces in the **packaged** build too, it is not a dev artifact.
2. **Does 1.9b block the merge?** Not yet decided. If it turns out to be a real shutdown defect,
   does #49 wait for a fix, or merge with a tracked follow-up issue? Ask the operator; do not assume.
3. **`docs/testing/PLAN.md` §3 has six items awaiting human ratification**, including
   `spine_weight` — the Impact axis of all 131 gap scores, currently a first-run *proposal*. Not
   smoke-blocking. Do not treat those scores as ratified until the operator confirms.
4. **Issue #52 (GitHub Support ticket to GC unreachable objects) needs the operator's account.** An
   agent cannot file it. Still open; it is the only item closing a real exposure.
5. **Issue #54 — retire the spike branches/worktrees** (`spike/app-plugins-webview`,
   `spike/app-plugins-webview-v2`, `spike/view-plugins-handshake`) — **only after #49 merges**. They
   remain the sole working reproduction if the smoke fails.

## On completion

1. Write the "Smoke run 2" results table into `spikes/SPIKE-RESULTS.md`, including any FAIL or
   ANOMALY verbatim — do not soften.
2. Update the State summary in [`docs/STATUS.md`](../STATUS.md) and append a session-log entry.
3. If all green: `gh pr ready 49`, then merge. If not: record precisely what failed and stop.
