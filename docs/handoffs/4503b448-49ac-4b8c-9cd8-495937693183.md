# Handoff — fix D-7, then run the packaged Part 2 smoke for PR #49

**Written:** 2026-08-15  ·  **Branch:** `feat/plugin-runtime` @ `e6decdc`  ·  **Session:** `4503b448-49ac-4b8c-9cd8-495937693183`

## ⏳ Background operation in flight

**None.** Everything from this session completed. The app was quit gracefully, `docker ps` is
**0 running**, no `app.exe` / `fleetd-serve.exe` / `cargo` processes remain. The remaining work is
**blocked on a human GUI session**, not on a machine.

CI has **not** been re-run against this session's three code/doc commits — nothing is pushed yet.
Check with `gh pr checks 49` after pushing.

## Goal

Land PR #49. Smoke run 2 completed **Part 1** and confirmed the pivotal fix, but surfaced four
defects. Two are fixed; **D-7 is the agreed next action**, then the **packaged Part 2** — which has
never been executed and is required before #49 can be marked ready.

## State

- **Canonical results:** [`spikes/SPIKE-RESULTS.md`](../../spikes/SPIKE-RESULTS.md) → **"Smoke run 2"**
  (findings D-1…D-6; D-7 is written up in this brief and in `docs/STATUS.md`)
- **Status doc:** [`docs/STATUS.md`](../STATUS.md) — State summary current as of 2026-08-15
- **Done this session** (4 commits, **not pushed**):
  - `55b0a5b` — **D-1 fixed**: `pluginSrc` now emits the Windows `http://ccplugin.localhost/…` form.
    Verified cold-start.
  - `2ab1b49` — **D-4 fixed**: `ShutdownGuard` ends the infinite `prevent_exit`/`exit(0)` loop.
    Verified in a watched window (exits ~1 s, 0.19 s CPU; was 309 s and never exiting).
  - `44ee6ad` — Smoke run 2 results table + findings
  - `e6decdc` — STATUS.md + CLAUDE.md pickup block
  - Filed [#61](https://github.com/adbarc92/command-center/issues/61) — loading-model UX (blank tabs,
    silent launch failures)
- **In flight (uncommitted):** nothing. Working tree clean.
- **Gates after the fixes:** `cargo test` 37 · `cargo fmt` clean · `clippy -D warnings` clean ·
  `npm test` 137 (19 files) · `npm run check` 353 files, 0/0.

## Successor autonomy

**`checkpoint-each`, operator-driven — settled with the operator this session and explicitly
endorsed by them.** Use the **`driving-interactive-smoke-tests`** skill
(`~/.claude/skills/driving-interactive-smoke-tests/`), written at their request from this session's
method. In short: one item at a time; state the literal action, the exact PASS string, and the FAIL
signatures *before* they click; ask with structured options; and **never record a result the
operator did not explicitly report**. Recording results and committing is autonomous once a result
is reported.

## Successor's next action

### 1. Fix D-7 — view-plugins receive no state (agreed with the operator)

`PluginSession.sendFullState()` throws `DataCloneError` posting Svelte 5 `$state` proxies through
`postMessage`:

```
bridge.ts:512 Uncaught DataCloneError: Failed to execute 'postMessage' on 'MessagePort'
    at PluginSession.sendFullState (bridge.ts:405)
    at PluginSession.onReady (bridge.ts:389)
```

`store.svelte.ts:34` declares `units = $state<Record<string, Unit>>({})`, so `toUnitLite`'s nested
fields (`history`, `findings`, `oracleFiles`) stay proxy-backed and are not structured-cloneable.
The throw lands **inside `onReady` before the tick timer starts**, so the plugin gets **no state and
no ticks** — the whole state channel is dead. This is why the reference plugin shows `no units`.

Fix with `$state.snapshot()` or a structural clone before `post()`. **Test-first, and drive the test
with real proxies** — `bridge.test.ts` uses plain fake objects, which is exactly why 137 tests
missed it. A fake-based test will pass against the broken code.

### 2. Decide D-2 — capability negotiation is inert *(unresolved, see Open questions)*

`grantedCapabilities` is computed at `loader.ts:140` and read **only by a test**
(`loader.test.ts:75`). `App.svelte:237` constructs `PluginBridge` without `capabilities`, so
`bridge.ts:594` falls back to `[...HOST_CAPABILITIES]` and ships the full host set. Observed live: a
plugin whose manifest requests only `log-append` was granted `real-launch-confirm` too.

### 3. Run Part 2 — the packaged smoke (never executed)

```powershell
$repo = 'D:\MajorProjects\CURRENT\command-center'
Set-Location "$repo\cockpit\ui"; npm run bundle          # long: budget 20+ min
# then run the packaged exe from target/release/
```

Repeat items 1.1–1.9 against the packaged build. **Part 2 needs no `CC_*` env vars** — that is the
point of it: it confirms `ccplugin://` and the child webview work with **no dev server**. Part 2 is
also the only way to confirm D-1's fix in the packaged path, since `pluginSrc`'s packaged branch is
what ships.

## Live decisions (settled)

- **`db74a47` is CONFIRMED and needs no further verification.** Measured: 1,127 samples at 1 Hz,
  **zero unresponsive**, across a real `compose build` and a 0→3→10 container ramp.
- **1.9b was a real shutdown defect, not a `tauri dev` artifact.** Settled with evidence; do not
  reopen it as an open question. Fixed and verified.
- **D-3 (fleetd has no CORS) is out of scope for #49.** Pre-existing on `main`;
  `git diff origin/main...HEAD` is empty for `api.ts` and `crates/fleetd/`. It BLOCKS smoke items
  1.2, 1.4a and 1.7 — record those as **BLOCKED**, never as FAIL, and do not chase them as #49 bugs.
- **Real-mode fleetd missions are authorised** by the operator (they approved spend this session).
  Note `mode:'real'` currently fails at provisioning anyway — the sandbox container cannot clone the
  private agent repo (`could not read Username for 'https://github.com'`). Use **tier `t2`** to reach
  `awaiting_oracle_approval`; **t1 auto-freezes the oracle** (`driver.rs:934`) and never parks.

## ⚠️ Open questions (unresolved — do NOT settle implicitly)

1. **Push and update PR #49?** The operator chose "commit" over "commit and push", but said "we need
   to focus on PR'ing the work already in-flight". **Four commits are sitting unpushed.** Confirm
   before pushing — do not assume either way.
2. **D-2 — fix, or ship #49 with capability negotiation inert?** Not decided. It is security-relevant
   (every plugin gets every host capability), so it should not be defaulted quietly.
3. **Does the remaining half of D-4 block the merge?** `stop_all_owned(30_000)` still runs
   synchronously inside the `RunEvent` callback, so exit is blocked for as long as teardown takes
   (~2.5 min with 10 containers up). Deliberately not bundled into `2ab1b49` (one change at a time).
   The process now *exits*; it does not exit *promptly*.
4. **Should the `docs/testing/PLAN.md` risk model be re-weighted?** Three of the four new defects
   share one shape — **a unit test passing on a function whose output is never wired to anything**.
   The gap is integration-boundary coverage, not unit coverage. The plan's §3 items (including
   `spine_weight`) still await human ratification, unchanged from the last handoff.
5. **Issue #52 (GitHub Support ticket to GC unreachable objects)** still needs the operator's
   account; an agent cannot file it. Unchanged.
6. **Issue #54 — retire the spike branches/worktrees** — still gated on #49 merging.

## Traps (updated — two of the previous handoff's premises were wrong)

1. **`docker ps` is the wrong instrument for Gate 5.** It shows only *running* containers. Run 1
   left `audience-{minio,postgres,redis}-1` in **`Created`** state, invisible to `docker ps`, and
   that residue broke run 2's first launch with a name conflict. **Assert on `docker ps -a`.**
2. **"Images are prebuilt, so no 20-minute build" is FALSE.** `compose build` runs regardless; it
   rebuilt two images this session.
3. **A stale pre-warm is not a warm build.** After `main` merged up, the `app` crate rebuilt cold at
   **16m22s**. Budget that for Part 2.
4. **Quit the cockpit gracefully, never `Stop-Process`,** during Gate 5 — a force-kill skips
   `stop_all_owned` and fabricates a teardown failure.
5. The old "a running dev app blocks any tauri rebuild" trap was a *symptom* of D-4 and should be
   largely gone since `2ab1b49`.

## On completion

1. Append **"Smoke run 2 — Part 2"** to `spikes/SPIKE-RESULTS.md`, verbatim on any FAIL or ANOMALY.
2. Rewrite the State summary in [`docs/STATUS.md`](../STATUS.md) and append a session-log entry.
3. If all green **and** D-7/D-2 are resolved: `gh pr ready 49`, then merge. Otherwise record exactly
   what failed and stop — a partial pass does not unblock #49.
