//! `fleetd` — the SP1 fleet daemon. Currently a stub; the Runner trait, runners,
//! lifecycle driver, and the HTTP/WS server land in the rest of Phase 1 and Phase 2.

fn main() {
    println!("fleetd (SP1 skeleton) — not yet wired up");
}
